

fun handleException(task: Int, error: Int, data: Int)
    # Handle the exception here
    # You can add your exception handling logic based on the task, error, and data parameters

fun handleSysCall(task: Int, sysCallNumber: Int)
    # Handle the system call here
    # You can add your system call handling logic based on the task and sysCallNumber parameters

fun handleInterrupt(interruptNumber: Int)
    # Handle the interrupt here
    # You can add your interrupt handling logic based on the interruptNumber parameter

fun kernelMain() -> Int

    initializeMemorySystem()

    main()
    return 0

    # val gs = new GfxScene()
    # val v1 = new GfxVertex(100.0, 100.0, 1.0, 0.0, 0.0, 0.0)
    # val v2 = new GfxVertex(200.0, 100.0, 1.0, 0.0, 255.0, 0.0)
    # val v3 = new GfxVertex(150.0, 200.0, 1.0, 255.0, 255.0, 255.0)
    # val v4 = new GfxVertex(150.0, 200.0, 1.0, 255.0, 0.0, 0.0)

    # for theta in 0..4095
    #     gs.clear()
    #     v1.x = 320.0 + 200.0 * cosPhase(theta)
    #     v1.y = 240.0 + 200.0 * sinPhase(theta)
    #     v2.x = 320.0 + 200.0 * cosPhase(theta + 1024)
    #     v2.y = 240.0 + 200.0 * sinPhase(theta + 1024)
    #     v3.x = 320.0 + 200.0 * cosPhase(theta + 2048)
    #     v3.y = 240.0 + 200.0 * sinPhase(theta + 2048)
    #     v4.x = 320.0 + 200.0 * cosPhase(theta + 3072)
    #     v4.y = 240.0 + 200.0 * sinPhase(theta + 3072)
    #     gs.addTriangle(false, v1, v2, v3)
    #     gs.addTriangle(false, v1, v3, v4)

    #     # gs.dump()

    #     gs.show()
    #     waitForVBlank()
    #     kprintf("Frame %d\n", theta)

    # kprintf("All done\n")
    # return 0